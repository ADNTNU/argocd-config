apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-api
spec:
  selector:
    matchLabels:
      app: dev-api
  replicas: 1
  template:
    metadata:
      labels:
        app: dev-api
    spec:
      hostNetwork: true
      containers:
        - name: dev-api
          image: danielneset/idata2306dev:dev-14fa434b216c5d99f3f6dad3d6642efbcdef928a
          ports:
            - containerPort: 8081
              protocol: TCP
          env:
            - name: PORT
              value: "8081"
            - name: DDL
              value: "update"
            - name: DB_HOST
              value: "localhost"
            - name: DB_PORT
              value: "3306"
            - name: DB_NAME
              value: "dev"
            - name: DB_USERNAME
              value: "devUser"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: devpass
                  key: secret-key
            - name: JWT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: jwtdev
                  key: secret-key

