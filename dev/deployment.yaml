apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-api
spec:
  selector:
    matchLabels:
      app: dev-api
  replicas: 1
  template:
    metadata:
      labels:
        app: dev-api
    spec:
      hostNetwork: true
      containers:
        - name: dev-api
          image: danielneset/idata2306dev:dev-b68bfb933b38f407a8d6af42b6597352a4a55d99
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
            - name: DB_HOST
              value: "localhost"
            - name: DB_PORT
              value: "3306"
            - name: DB_NAME
              value: "dev"
            - name: DB_USERNAME
              value: "devUser"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: devpass
                  key: secret-key
